%h2 Rejestracja
.container
  = simple_form_for(resource, as: resource_name, url: registration_path(resource_name), html: {class: 'form-horizontal' }) do |f|
    = f.error_notification
    .form-group
      = f.label "Email", class: 'col-lg-2'
      .col-lg-10= f.input_field :email
    .form-group
      = f.label "Hasło", class: 'col-lg-2'
      .col-lg-10= f.input_field :password
    .form-group
      = f.label "Powtórz hasło", class: 'col-lg-2'
      .col-lg-10= f.input_field :password_confirmation
    = f.button :submit, "Zarejestruj się", class: 'btn-primary'
